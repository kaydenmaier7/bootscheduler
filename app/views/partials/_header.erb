<% if current_user %>
  
	<%= link_to 'Logout', session_path(session[:id]), method: :delete %>
	<%= link_to 'Profile Page', boot_path(current_user.id) %>

<% else %>

	<%= link_to 'Login', new_session_path %>  
	<%= link_to 'Sign Up', new_boot_path %>
<% end %>